[package]
name = "ray_tracer"
version = "0.1.0"
authors = ["John Shiver <johneshiver@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
num = "*"
uuid = { version = "0.8.1", features = ["serde", "v4"] }
